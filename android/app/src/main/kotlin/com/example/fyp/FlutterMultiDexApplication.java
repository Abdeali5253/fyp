package com.example.fyp; // Make sure this matches your package name

import androidx.multidex.MultiDexApplication;

public class FlutterMultiDexApplication extends MultiDexApplication {
    public void onCreate() {
        super.onCreate();
        // Any other application level initialization code can go here.
    }
}
